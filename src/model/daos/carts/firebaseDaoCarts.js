import { FirebaseManager } from "../../managers/firebase.manager.js"

class FirebaseDAOCarts extends FirebaseManager {
	constructor(nameCollection) {
		super(nameCollection)
	}
}

export { FirebaseDAOCarts }
